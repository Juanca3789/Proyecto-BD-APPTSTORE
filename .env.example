# Docker Compose — copia a .env en la raíz del repo
# cp .env.example .env

# URL de la API (build-time de Vite al construir la imagen UI)
VITE_API_BASE_URL=http://localhost:8080

# Puerto en el host donde se expone la UI (contenedor nginx sigue en :80)
UI_PORT=3000
